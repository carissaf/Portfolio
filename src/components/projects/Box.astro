---
import Title from "../Title.astro";
const {title, useTitle} = Astro.props
---

<div class="border rounded-xl px-5 py-4 min-w-96">
    {useTitle ? <Title title={title} className=""/> : <h3 class="text-base opacity-50 mb-1">{title}</h3>}
    <slot/>
</div>
